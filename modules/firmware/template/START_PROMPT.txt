Analyze the authorized firmware configured in `target/TARGET.toml`. Identify/extract it, build an attack-surface map, then continue with prioritized deep dives.

Read `orchestration.max_parallel_agents` from TARGET.toml and never exceed that many concurrently executing delegated tasks; default to 2 when absent.

Keep the primary context small, research selectively, and independently validate important findings. Static analysis only.
