Analyze the authorized API from `target/TARGET.toml`. Start with scope/config verification and attack-surface mapping, then continue into prioritized security testing.

Read `orchestration.max_parallel_agents` from TARGET.toml and never exceed that many concurrently executing delegated tasks; default to 2 when absent.

Keep the primary context small, route all target traffic through `tools/api_request.py`, and independently validate important findings. Maintain findings/ and reports/subagents/.
