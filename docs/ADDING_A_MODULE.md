# Adding a Module

Create `modules/<id>/module.toml` and a template. Reuse/add dependency IDs in `dependencies/catalog.toml`; special installers go once into `lib/dependencies.sh`. No central module registry. Run validation and repo-guard.
