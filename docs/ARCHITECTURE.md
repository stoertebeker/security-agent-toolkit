# Architecture

Toolkit repo only. `toolkit init` creates external workspaces. OpenCode is core. Runtime goes under `~/.local/share/security-agent-toolkit`; native utilities use APT. No emulation stack.
