# PE / EXE malware analysis

Experimental static-first module `pe`. v0 records file identity, hashes, strings, IOC/capability leads and supports focused AI review without executing the target. Full PE table parsing and richer Ghidra automation are the next hardening steps.
